# Lakona.Game.Cluster.Rpc.Serializer.Json

JSON serializer adapter for the Lakona.Game cluster RPC channel.

```csharp
server.UseClusterRpc(
    clusterTransport,
    JsonClusterRpcSerializer.Default);
```
