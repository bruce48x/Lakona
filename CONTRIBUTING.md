# Contributing To Lakona

This repository is in its initial monorepo migration phase. Build from the repository root with:

```powershell
dotnet build Lakona.sln
```

Run tests with:

```powershell
dotnet test Lakona.sln --no-build
```

