baseURL = "https://bruce48x.github.io/Lakona/"
languageCode = "en-us"
title = "Lakona"

[params]
description = "A C# full-stack game server framework with shared contracts, typed RPC, actor-owned state, reliable push, live logic updates, runtime diagnostics, and no database lock-in."
repo = "https://github.com/bruce48x/Lakona"

[markup]
  [markup.highlight]
    noClasses = false
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
